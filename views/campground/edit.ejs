<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>Edit the camp</h1>
    <form action="/campgrounds/ <%= results[0].id %>?_method=PUT " method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="title" value="<%= results[0].title %>">
        </div>

        <div>
            <label for="location">Location</label>
            <input type="text" id="location" name="location" value="<%= results[0].location %>">
        </div>

        <button type="submit">Edit the campground</button>
        <br>
        <a href="/campgrounds/<%= results[0].id  %>">Back to campgrounds</a>
    </form>
</body>

</html>